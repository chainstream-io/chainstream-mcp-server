apiVersion: v1
kind: Service
metadata:
  name: mcp-server-service
  namespace: chainstream-mcp
  labels:
    app: mcp-server
spec:
  type: ClusterIP
  selector:
    app: mcp-server
  ports:
  - name: http
    port: 80
    targetPort: 3030
    protocol: TCP
